const hamburger = documnet.getElementById("hamburger");
const navlinks = documnet.getElementById("navlinks");


hamburger.addEventListener("click", ()=>{
      navlinks.classList.toggle("navlinks-active");


})
